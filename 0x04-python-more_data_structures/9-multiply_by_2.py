#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    new_list = a_dictionary[:]
    for row in range(len(new_list))
        new_list[row] = list(map(lambda x: x * 2, new_list[i]))
    return new_list
